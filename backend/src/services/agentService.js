const NoteJob = require('../models/NoteJob.model');

const MOCK = process.env.MOCK_AGENT === "true";

const MOCK_RESPONSE = {
    final_md: '# Transformers and Self-Attention\n\n## Introduction to Transformers and the Need for Self-Attention\n\nTraditional **Recurrent Neural Networks (RNNs)** and their variants like **Long Short-Term Memory (LSTMs)** process sequences one element at a time. This sequential nature inherently limits **parallelization**, making them computationally expensive and slow for very long sequences. Furthermore, RNNs struggle with capturing **long-range dependencies**, where information from early parts of a sequence needs to influence later parts. This is primarily due to issues like vanishing or exploding gradients, which make it difficult to propagate relevant information effectively over many time steps.\n\nThe **Transformer architecture** emerged as a revolutionary solution designed to overcome these fundamental limitations. It completely eschewed recurrence and convolutions, instead relying entirely on **attention mechanisms** to process sequences. This novel approach allowed for significant advancements in tasks such as machine translation and natural language understanding by enabling more efficient and effective handling of long sequences.\n\nAt its core, the Transformer utilizes **self-attention**. This mechanism allows the model to weigh the importance of different parts of the *same* input sequence when processing each element. For instance, when the model processes a specific word in a sentence, self-attention enables it to look at and assign varying degrees of importance to all other words in that sentence to better understand the current word\'s context.\n\nThis paradigm shift was famously introduced in the 2017 paper "Attention Is All You Need," which demonstrated that attention mechanisms alone could achieve state-of-the-art results, paving the way for modern large language models.\n\n> Transformers leverage self-attention to overcome RNN limitations, enabling efficient parallel processing and robust handling of long-range dependencies.\n\n## The Mechanics of Self-Attention\n\nThe **self-attention** mechanism enables a model to weigh the importance of different words in an input sequence relative to each other when processing a single word. This process begins by transforming each input word\'s embedding into three distinct vectors: **Query (Q)**, **Key (K)**, and **Value (V)**. These are typically derived by multiplying the input embedding with three separate learnable weight matrices (W_Q, W_K, W_V). Conceptually, the Query vector represents what the current word is looking for, the Key vector represents what a word offers, and the Value vector contains the actual information to be aggregated.\n\nThe core of self-attention is the **scaled dot-product attention**. For each Query vector, its similarity to all Key vectors in the sequence is computed using a dot product: `Q ⋅ Kᵀ`. This dot product measures how well a Query matches a Key. The results are then divided by the square root of the dimension of the Key vectors (`√dₖ`) to prevent large dot product values from pushing the softmax function into regions with tiny gradients. Finally, a **softmax** function is applied to these scaled scores, converting them into probabilities or **attention weights** that sum to one, indicating the relative importance of each word.\n\nThese attention weights are then used to create a **weighted sum** of the Value vectors. For each position, its output vector is calculated by multiplying each Value vector by its corresponding attention weight and summing these weighted Values. This means that words with higher attention weights contribute more significantly to the output representation of the current word, effectively allowing the model to "focus" on relevant parts of the input.\n\nTo enhance the model\'s ability to capture diverse relationships, **Multi-Head Attention** is employed. Instead of performing self-attention once, the input Q, K, and V matrices are split into multiple "heads." Each head independently performs the scaled dot-product attention calculation using its own set of learned Q, K, and V weight matrices. This allows the model to attend to different aspects of the input simultaneously and learn various types of dependencies (e.g., syntactic, semantic). The outputs from all heads are then concatenated and linearly transformed to produce the final output.\n\n> Self-attention computes context-aware representations by dynamically weighting input elements using Q, K, and V vectors, scaled dot-products, and a final weighted sum, with Multi-Head Attention enabling diverse focus.\n\n## Transformer Architecture and its Components\n\nThe original Transformer model employs an **encoder-decoder structure** designed for sequence-to-sequence tasks, such as machine translation. The **encoder** stack processes the input sequence, transforming it into a continuous representation. This representation is then passed to the **decoder** stack, which generates the output sequence one token at a time. Information flows from the input embeddings, through multiple identical encoder layers, then through multiple identical decoder layers, finally passing through a linear layer and softmax to produce output probabilities.\n\nUnlike recurrent neural networks (RNNs), Transformers process input sequences in parallel, meaning they inherently lack a mechanism to understand the order of tokens. To address this, **Positional Encoding** is added to the input embeddings. These are vectors containing information about the relative or absolute position of each token in the sequence. By summing these positional encodings with the token embeddings, the model gains awareness of token order, which is crucial for understanding sentence structure and meaning.\n\nWithin each encoder and decoder layer, after the attention sub-layer, there is a **Feed-Forward Network (FFN)**. This FFN consists of two linear transformations with a ReLU activation in between. It operates independently and identically on each position (token) in the sequence. The FFN\'s role is to introduce non-linearity and allow the model to learn complex, position-wise transformations of the representations, further refining the information processed by the attention mechanism.\n\nTo facilitate the training of deep Transformer networks and prevent issues like vanishing gradients, **Residual Connections** and **Layer Normalization** are used throughout the architecture. A residual connection adds the input of a sub-layer to its output, allowing gradients to flow directly through the network. Layer Normalization then normalizes the activations across the features for each sample, stabilizing the training process and speeding up convergence. This "add & norm" pattern is applied after every sub-layer (attention and FFN) in both the encoder and decoder.\n\nThe core of the Transformer\'s processing power lies in its attention mechanisms. In the encoder, each layer contains a **multi-head self-attention** mechanism, allowing it to weigh the importance of all other tokens in the input sequence when processing a single token. The decoder also uses multi-head self-attention, but it\'s "masked" to prevent attending to future tokens. Additionally, the decoder includes a **cross-attention** sub-layer, where the queries come from the previous decoder layer, and the keys and values come from the output of the encoder stack. This enables the decoder to focus on relevant parts of the input sequence while generating the output.\n\n> The Transformer integrates parallel processing with positional awareness, non-linear transformations, and robust training techniques to effectively handle sequential data.\n\n## Applications, Limitations, and Practice Questions\n\n**Transformers** have revolutionized deep learning due to several key advantages. Their core mechanism, **self-attention**, allows them to effectively capture **long-range dependencies** within sequences, meaning they can relate distant parts of an input without losing context. This architecture also enables high **parallelization** during training, significantly speeding up computation compared to recurrent networks. Consequently, Transformers have achieved state-of-the-art performance across diverse tasks.\n\nTheir impact is evident in numerous applications. In **Natural Language Processing (NLP)**, Transformers power advanced systems for machine translation, text generation (e.g., large language models), sentiment analysis, and question answering. Beyond text, they have expanded into **Computer Vision (CV)** with models like **Vision Transformers (ViT)**, demonstrating strong performance in image classification, object detection, and segmentation by treating image patches as sequences.\n\nDespite their power, Transformers face certain limitations. The **computational cost** of self-attention scales quadratically with sequence length, making them resource-intensive for very long inputs. Training these models also typically requires extremely large datasets and significant computational resources. Furthermore, their complex internal workings can lead to **interpretability** issues, making it challenging to understand precisely *why* a model makes a particular prediction.\n\n### Practice Questions\n\n1.  What is the primary advantage of **self-attention** in Transformers for handling long sequences compared to traditional recurrent neural networks?\n2.  List two distinct applications of Transformers in **Natural Language Processing (NLP)** and one in **Computer Vision (CV)**.\n3.  Explain one major limitation of Transformers regarding their computational efficiency for very long input sequences.\n4.  Briefly describe the role of Query (Q), Key (K), and Value (V) matrices in the self-attention mechanism.\n\n> Transformers offer unparalleled sequence modeling capabilities but require careful resource management and further research into interpretability.\n'
,
    mode: "closed_book",
    needs_research: false,
    needs_rag: false,
};

async function triggerAgent(jobId, topic, fileBase64 = "", contextProvided = false, filename = "") {
    await NoteJob.findByIdAndUpdate(jobId, { status: "running" });

    try {
        let data;

        if (MOCK) {
            await new Promise((res) => setTimeout(res, 3000));
            data = MOCK_RESPONSE;
        } else {
            const res = await fetch(`${process.env.AGENT_URL}/generate`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                    topic,
                    raw_input:        Buffer.from(fileBase64 || "", "base64").toString("utf-8"),
                    context_provided: contextProvided,
                    resume:           true,
                }),
            });
            data = await res.json();
        }
        await NoteJob.findByIdAndUpdate(jobId, {
            status:        "done",
            finalMd:       data.final_md,
            mode:          data.mode,
            needsResearch: data.needs_research,
            needsRag:      data.needs_rag,
        });

    } catch (err) {
        await NoteJob.findByIdAndUpdate(jobId, {
            status: "failed",
            error:  err.message,
        });
    }
}

module.exports = { triggerAgent };


